+++
title = "1.2.2"
date = 2026-08-26T12:00:00Z
[extra]
color = "magenta"
+++

#### Fixes

- Running the installer on a non-`main` branch (e.g. `dev`) now updates the repository against its upstream instead of skipping the update, keeping the discard prompt when local changes exist. Local-only branches and detached HEADs still skip safely.
